<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-mybatis.org/DTD Mapper 3.0//EN"
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd"
>

<mapper namespace="com.ssafy.materip.model.dao.ParticpantsDao">
	 <resultMap id="participants" type="participants">
		 <result column="board_id" property = "boardId"/>
		 <result column="user_id" property = "userId"/>
	 </resultMap>
	
	<insert id="addParticipants"  parameterType="participants">
		INSERT INTO participants
		(board_id, user_id)
		VALUES (#{board_id}, #{user_id})
	</insert>

	<delete id="removeParticipants" parameterType="participants">
		DELETE FROM participants
		WHERE board_id = #{board_id} and user_id = #{user_id}
	</delete>

	<select id="getParticipants" parameterType="int" resultType="participants" resultMap="participants">
		SELECT user_id
		FROM participants
		WHERE board_id = #{board_id};
	</select>

	<select id="getParticipantsCnt" parameterType="int" resultType="int">
		SELECT COUNT(*)
		FROM participants
		WHERE board_id = #{board_id};
	</select>
</mapper>